command: query

table: repo_repo_count_100

query: >
  SELECT * FROM (
    SELECT
      a.repository_url AS repo1,
      b.repository_url AS repo2,
      COUNT(1) AS c
    FROM [ghwatch3.repo_actor_createdat_100] a
    RIGHT JOIN EACH [ghwatch3.repo_actor_createdat_100] b
    ON a.actor = b.actor
    GROUP EACH BY 1, 2 )
  WHERE c > 10 LIMIT 10000;
